const attributes = {
  blockId: {
    type: 'string',
  },
  interval: {
    type: 'number',
    default: 5000,
  },
  isInterval: {
    type: 'boolean',
    default: false,
  },
};

export default attributes;